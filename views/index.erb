<ul>
  <% @memos.each do |memo| %>
  <li>
    <a href="/memos/<%= memo[:id] %>">メモ<%= memo[:id] %>：<%= escape(memo[:name]) %></a>
  </li>
  <% end %>
</ul>

<a href='/memos/new' class="button">追加</a>
